# Glimpse

Party game skill. Host Cortex. Guests join at `/play/glimpse/:invite` with no install.

`run_skill` actions: `open_ui`, `status`, `create_room`, `catalog`.

Normative engine: `lib/cortexGlimpse/`.
